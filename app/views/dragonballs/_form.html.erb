<%= form_for(dragonball) do |f| %>
  <% if dragonball.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dragonball.errors.count, "error") %> prohibited this dragonball from being saved:</h2>

      <ul>
      <% dragonball.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
